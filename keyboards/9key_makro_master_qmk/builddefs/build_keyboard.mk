# Minimal QMK build_keyboard.mk template
# Add your keyboard-specific build rules here

# Example: include common QMK rules
# include $(QMK_PATH)/builddefs/common_rules.mk

# Placeholder target to avoid errors
all:
	@echo "build_keyboard.mk loaded. Customize as needed."
